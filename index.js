

function theBeatlesPlay (name, instruments) {
  var array = [];
  for (let i = 0; i < length.name; i++  ) {
    array[i] =  name[i] + instruments;
    console.log (array[i]);
  }
  return ('done');
}